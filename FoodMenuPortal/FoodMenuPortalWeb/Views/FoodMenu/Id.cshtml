@model FoodMenuPortalWeb.ViewModels.FoodMenuItemListViewModel
@{
	 Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <div>
		  @{  for (int i = 0; i < Model.FoodMenuItems.Count; i++)
				{
					 <div>@Model.FoodMenuItems[i].Name : @Model.FoodMenuItems[i].Price</div>
				}
        }

        <div>@Html.ActionLink("Add New FoodMenuItem", "CreateItem")</div>
    </div>
</body>
</html>
